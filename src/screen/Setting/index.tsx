import {Text, View} from 'react-native';
import React from 'react';
import {styles} from './DefaultType';

const Setting = () => {
  return (
    <View style={styles.screen}>
      <Text style={styles.label}>Setting</Text>
    </View>
  );
};

export default Setting;
